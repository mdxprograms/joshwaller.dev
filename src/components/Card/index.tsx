import React from "react";

import styles from "./card.module.css";

const Card = () => (
  <div className={styles.card}>
    <h5 className={styles.cardTitle}>Test Card</h5>
  </div>
);

export default Card;
